# Dipendenze runtime — Fase 1 (MVP notifica grezza). Target: Python 3.12 sul VPS.
# NB: models.py e db.py usano solo la stdlib (sqlite3, dataclasses): nessuna dep.
# Le voci sotto servono ai moduli in arrivo (scraper, config loader, notifier).

# --- scraper.py: fetch + parsing HTML (CLAUDE.md §7, §8) ---
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2

# --- config loader (comuni.yaml, scoring.yaml) ---
PyYAML>=6.0

# --- notifier.py: invio Telegram ---
# Decisione §7: riuso del codice del bot Kraken (python-telegram-bot). Il notifier
# è però un wrapper sottile one-way: in alternativa basta la Bot API via httpx.
# Si fissa la scelta quando si implementa notifier.py; per ora dep non aggiunta
# per non trascinare un pacchetto async pesante prima del tempo.
