# Reference

Descrizione asciutta e completa: comandi CLI/Telegram (`/scan`, `/status`,
`/lotto <id>`, `/soglia <param> <val>`), schema di `db/aste.sqlite`, formato del
JSON estratto dalle perizie (CLAUDE.md §3, §6).
